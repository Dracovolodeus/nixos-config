--- a/src/global/Configs.cpp
+++ b/src/global/Configs.cpp
@@ -47,2 +47,8 @@
     QString FindCoreRealPath() {
+        // Prefer the NixOS security wrapper when it is available.
+        QString nixosCore = QStandardPaths::findExecutable("throne-core");
+        if (!nixosCore.isEmpty()) {
+            return nixosCore;
+        }
+
         auto fn = QApplication::applicationDirPath() + "/ThroneCore";
--- a/src/ui/mainWindow/mainwindow_setup.cpp
+++ b/src/ui/mainWindow/mainwindow_setup.cpp
@@ -210,2 +210,1 @@
-    auto core_path = QApplication::applicationDirPath() + "/";
-    core_path += "ThroneCore";
+    auto core_path = Configs::FindCoreRealPath();
--- a/src/ui/mainWindow/mainwindow_system.cpp
+++ b/src/ui/mainWindow/mainwindow_system.cpp
@@ -195,2 +195,11 @@
     if (Configs::IsAdmin()) return true;
+    QMessageBox::critical(
+        GetMessageBoxParent(),
+        tr("Unable to elevate privileges when installed with Nix"),
+        tr("The Nix store is read-only, so ThroneCore cannot set suid. On NixOS, enable programs.throne.tunMode instead."),
+        QMessageBox::Ok
+    );
+    return false;
+    // Preserve the upstream implementation below to reduce patch conflicts.
+
 #ifdef Q_OS_LINUX
